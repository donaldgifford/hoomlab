# Summary

<!-- What changed and why. Link the related issue: Fixes #123 / Refs #123 -->

## Release impact

<!--
Releases are driven by this PR's semver label on merge to main:
  major / minor / patch — bump the version, publish image + chart
  dont-release          — no version bump; chart-only changes still
                          publish if Chart.yaml's version advanced
Pick exactly one.
-->

- [ ] Labeled `major`, `minor`, `patch`, or `dont-release`

## Test plan

<!-- How you verified the change: unit tests, `just helm-test`, k3d loop, ... -->
