---
# chart-testing (ct) does not publish a JSON schema — no yaml-language-server
# modeline. See https://github.com/helm/chart-testing/issues for status.
chart-dirs:
  - charts
target-branch: main
check-version-increment: false
validate-maintainers: false
validate-chart-schema: false
lint-conf: charts/.yamllint.yml
