apiVersion: v1
kind: Service
metadata:
  name: {{ include "hoomlab.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "hoomlab.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.metricsPort }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "hoomlab.selectorLabels" . | nindent 4 }}
